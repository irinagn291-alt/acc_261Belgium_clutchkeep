import Foundation

/// Role: Hen. Desert dune tokens. Hex lives only here. Type is SF Pro via Font.system.
enum RoostInk {
    static let background = "#F5FAFA"
    static let surface = "#FDFEFE"
    static let ink = "#183936"
    static let accent = "#25C1B4"
    static let muted = "#547875"
}

/// Role: Hen. Contact on Settings. Not a WebView.
enum RoostLink {
    /// Programmer constant — the contact URL is fixed in the spec.
    static let contact = URL(string: "https://clutchkeep-roost.pro/contact-us")!
}
